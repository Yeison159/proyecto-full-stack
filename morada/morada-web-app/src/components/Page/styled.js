import styled from 'styled-components'
import {MENU_HEIGH} from "../../constantes/styles";

export const PageWrapper = styled.section`
  margin: 10px 15px ${MENU_HEIGH + 10}px;
  overflow-y: auto;
`;
